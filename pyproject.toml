[project]
name = "tds-ga0-vercel"
version = "0.1.0"
description = "FastAPI latency API"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic>=2.9.0",
]

[tool.vercel]
entrypoint = "api.latency:app"